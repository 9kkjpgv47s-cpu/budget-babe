DATABASE_URL="file:./dev.db"
# Required in production (32+ chars). Dev uses a built-in default if unset.
# SESSION_PASSWORD=

# Optional: Plaid (https://dashboard.plaid.com) — enables /plaid bank link + transaction sync
# PLAID_CLIENT_ID=
# PLAID_SECRET=
# PLAID_ENV=sandbox
